#include <bits/stdc++.h>
using namespace std;

int ans = 1e9;

int dfs(vector<int> &l, vector<int> &flag, int a, int b, int c, int d, int n, int cu) {
    if(cu == n) {
        
    }
    for(int i = 0; i < 4; i++) {

    }
}

int main() {
    int n, a, b, c;
    cin >> n >> a >> b >> c;
    vector<int> l(n);
    vector<int> flag(8);
    for(int i = 0; i < n; i++) cin >> l[i];
    
}