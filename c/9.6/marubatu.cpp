#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<vector<int>> b(2, vector<int>(3));
    vector<vector<int>> c(3, vector<int>(2));
    for(int i = 0; i < 2; i++) {
        for(int j = 0; j < 3; j++) {
            cin >> b[i][j];
        }
    }
    for(int i = 0; i < 3; i++) {
        for(int j = 0; j < 2; j++) {
            cin >> c[i][j];
        }
    }

    
}